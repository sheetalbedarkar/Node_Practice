let a = 5;
let b = 10;

/* Using Normal String */
console.log('Result is : ' + ( a + b ) + ' and not ' + ( 2 * a + b ) + '.');

/* Using Template literals */
console.log(`Result is : ${a + b} and not ${2 * a + b}.`);
